# Console configuration. Copy to .env.local.
# Placeholders only — never commit a filled-in file.

# Base URL of pw-ongoingrec-backend. Exposed to the browser, so it must be a
# public URL in any deployed environment; it carries no secret.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api/v1
